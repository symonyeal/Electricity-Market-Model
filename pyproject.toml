[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
addopts = "-p no:cacheprovider"
markers = ["slow: clears the full pglib-uc MIP benchmark"]

[tool.ruff]
line-length = 100
# Archived material is historical and is not maintained against current lint rules.
exclude = ["_archive"]

[tool.ruff.lint]
# E501 is not in ruff's default selection, so line-length above is a formatting
# setting until it is asked for. This project writes to 100 columns; select it
# so the check enforces what the setting states.
extend-select = ["E501"]
# E741 rejects l, I and O as names. This project names variables after the
# symbols in the cited papers, where single letters are the notation.
# I001 orders and wraps imports. Ruff's only wrap mode puts one name per line,
# which fights the compact register the rest of these files are written in.
ignore = ["E741", "I001"]
